export enum SearchDisplayType {
  NEW_FORMAT = 1,
  OLD_FORMAT = 2,
  AS_INPUT_FORMAT = 4,
  BOTH_NEW_OLD = 5,
  BOTH_OLD_NEW = 6,
}

export enum ReverseDisplayType {
  NEW_FORMAT = 1,
  OLD_FORMAT = 2,
  TWO_OBJECTS = 4,
  BOTH_NEW_OLD = 5,
  BOTH_OLD_NEW = 6,
}

export enum MigrateType {
  OLD_TO_NEW = 1,
  NEW_TO_OLD = 2,
}
